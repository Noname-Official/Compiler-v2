[workspace]
members = ["lexer", "parser", "parser/derive"]

[package]
name = "compiler_v2"
version = "0.1.0"
edition = "2021"

[dependencies]
lexer = { path = "lexer" }
parser = { path = "parser", features = ["ast"] }
